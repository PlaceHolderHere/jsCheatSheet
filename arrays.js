let products = ["apples", "oranges"]
console.log(products[0])  // 1st item in the array

products[3] = "banana"  // Adding an item at any index
console.log(products)

products[2] = 1 // arrays can have multiple data types in them
console.log(products)

console.log(products.length)